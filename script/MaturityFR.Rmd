---
title: ' '
output:
  html_document: default
  word_document: default
  pdf_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

<html>
<head>
    <style>
  body {
        height: 842px;
        width: 595px;
        /* to centre page on screen*/
        margin-left: auto;
        margin-right: auto;
        font-family: sans-serif;
        font-size: 8pt;
    }
    </style>
  

<title>Page Title</title>

</head>

<body>

```{r start, echo=F, resultas='asis', warning=F, message=F}

library(dplyr, warn.conflicts = FALSE)
library(readxl, warn.conflicts = FALSE)
library(knitr, warn.conflicts = FALSE)
library(pander, warn.conflicts = FALSE)
library(kableExtra, warn.conflicts = FALSE)
library(magick, warn.conflicts = FALSE)
library(jpeg, warn.conflicts = FALSE)
library(png, warn.conflicts = FALSE)
library(imager, warn.conflicts = FALSE)
library(cowplot)
library(ggplot2)

#Import des jeux de données
Dataphoto <- read_excel("../data/photo_mat.xlsx",sheet=1)
#Dataphoto = jeu de données avec les photos

Datascale <- read_excel("../data/matu_scale.xlsx",sheet=1)
#Datascale = jeu de données avec les phases de maturité

Datataille <- read_excel("../data/matu_scale.xlsx",sheet=4)
#Datataille = jeu de données avec les tailles de 1ère maturité

```

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>ANCHOIS / <i>Engraulis encrasicolus</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Engraulis encrasicolus",Sexe=="F")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Anchois.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Engraulis encrasicolus",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Engraulis encrasicolus",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Engraulis encrasicolus",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Engraulis encrasicolus",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Engraulis encrasicolus",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Engraulis encrasicolus",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Engraulis encrasicolus",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Engraulis encrasicolus",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Engraulis encrasicolus",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Engraulis encrasicolus",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Engraulis encrasicolus",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Engraulis encrasicolus",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>ANCHOIS / <i>Engraulis encrasicolus</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Engraulis encrasicolus",Sexe=="M")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Anchois.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Engraulis encrasicolus",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Engraulis encrasicolus",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Engraulis encrasicolus",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Engraulis encrasicolus",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Engraulis encrasicolus",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Engraulis encrasicolus",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Engraulis encrasicolus",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Engraulis encrasicolus",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Engraulis encrasicolus",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Engraulis encrasicolus",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Engraulis encrasicolus",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Engraulis encrasicolus",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>BAR / <i>Dicentrarchus labrax</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Dicentrarchus labrax",Sexe=="F")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Bar.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Dicentrarchus labrax",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Dicentrarchus labrax",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Dicentrarchus labrax",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Dicentrarchus labrax",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Dicentrarchus labrax",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Dicentrarchus labrax",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Dicentrarchus labrax",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Dicentrarchus labrax",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Dicentrarchus labrax",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Dicentrarchus labrax",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Dicentrarchus labrax",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Dicentrarchus labrax",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>BAR / <i>Dicentrarchus labrax</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Dicentrarchus labrax",Sexe=="M")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Bar.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Dicentrarchus labrax",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Dicentrarchus labrax",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Dicentrarchus labrax",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Dicentrarchus labrax",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Dicentrarchus labrax",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Dicentrarchus labrax",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Dicentrarchus labrax",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Dicentrarchus labrax",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Dicentrarchus labrax",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Dicentrarchus labrax",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Dicentrarchus labrax",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Dicentrarchus labrax",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>BAUDROIE<br></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE1<-Datataille %>% filter(Species=="Lophius piscatorius",Sexe=="F")
TAILLE11 <- TAILLE1$Fishbase
TAILLE111 <- TAILLE1$Species
TAILLE2<-Datataille %>% filter(Species=="Lophius budegassa",Sexe=="F")
TAILLE22 <- TAILLE2$Fishbase
TAILLE222 <- TAILLE2$Species
phaseT <- c(TAILLE11,TAILLE22)
phaseTT <- c(TAILLE111,TAILLE222)

TT <- bind_cols(phaseTT,phaseT)

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "")
colnames(tbl_img)<-c("Sexe","Photo")

colnames(TT)<-c("Espèce","Taille de 1ère maturité")
kbl(TT, align="c") %>%
  kable_styling(full_width = F, position = "float_right")%>% column_spec(1, width="4cm")%>%kable_styling(font_size = 10)

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(2,image ="../data/Image poisson/Baudroie.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Lophius sp.",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Lophius sp.",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Lophius sp.",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Lophius sp.",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Lophius sp.",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Lophius sp.",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Lophius sp.",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Lophius sp.",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Lophius sp.",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Lophius sp.",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Lophius sp.",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Lophius sp.",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>BAUDROIE<br></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE1<-Datataille %>% filter(Species=="Lophius piscatorius",Sexe=="M")
TAILLE11 <- TAILLE1$Fishbase
TAILLE111 <- TAILLE1$Species
TAILLE2<-Datataille %>% filter(Species=="Lophius budegassa",Sexe=="M")
TAILLE22 <- TAILLE2$Fishbase
TAILLE222 <- TAILLE2$Species
phaseT <- c(TAILLE11,TAILLE22)
phaseTT <- c(TAILLE111,TAILLE222)

TT <- bind_cols(phaseTT,phaseT)

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "")
colnames(tbl_img)<-c("Sexe","Photo")

colnames(TT)<-c("Espèce","Taille de 1ère maturité")
kbl(TT, align="c") %>%
  kable_styling(full_width = F, position = "float_right")%>% column_spec(1, width="4cm")%>%kable_styling(font_size = 10)

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(2,image ="../data/Image poisson/Baudroie.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Lophius sp.",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Lophius sp.",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Lophius sp.",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Lophius sp.",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Lophius sp.",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Lophius sp.",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Lophius sp.",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Lophius sp.",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Lophius sp.",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Lophius sp.",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Lophius sp.",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Lophius sp.",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>DORADE ROSE / <i>Pagellus bogaraveo</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Pagellus bogaraveo",Sexe=="F")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Dorade rose.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Pagellus bogaraveo",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Pagellus bogaraveo",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Pagellus bogaraveo",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Pagellus bogaraveo",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Pagellus bogaraveo",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Pagellus bogaraveo",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Pagellus bogaraveo",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Pagellus bogaraveo",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Pagellus bogaraveo",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Pagellus bogaraveo",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Pagellus bogaraveo",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Pagellus bogaraveo",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>DORADE ROSE / <i>Pagellus bogaraveo</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Pagellus bogaraveo",Sexe=="M")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Dorade rose.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Pagellus bogaraveo",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Pagellus bogaraveo",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Pagellus bogaraveo",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Pagellus bogaraveo",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Pagellus bogaraveo",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Pagellus bogaraveo",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Pagellus bogaraveo",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Pagellus bogaraveo",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Pagellus bogaraveo",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Pagellus bogaraveo",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Pagellus bogaraveo",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Pagellus bogaraveo",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>GADIDAE<br></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE1<-Datataille %>% filter(Species=="Merlangius merlangus",Sexe=="F")
TAILLE11 <- TAILLE1$Fishbase
TAILLE111 <- TAILLE1$Species
TAILLE2<-Datataille %>% filter(Species=="Melanogrammus aeglefinus",Sexe=="F")
TAILLE22 <- TAILLE2$Fishbase
TAILLE222 <- TAILLE2$Species
TAILLE3<-Datataille %>% filter(Species=="Gadus morhua",Sexe=="F")
TAILLE33 <- TAILLE3$Fishbase
TAILLE333 <- TAILLE3$Species
phaseT <- c(TAILLE11,TAILLE22,TAILLE33)
phaseTT <- c(TAILLE111,TAILLE222,TAILLE333)

TT <- bind_cols(phaseTT,phaseT)

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "")
colnames(tbl_img)<-c("Sexe","Photo")

colnames(TT)<-c("Espèce","Taille de 1ère maturité")
kbl(TT, align="c") %>%
  kable_styling(full_width = F, position = "float_right")%>% column_spec(1, width="4cm")%>%kable_styling(font_size = 8)

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(2,image ="../data/Image poisson/Gadidae.jpg", width="5cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Gadidae sp.",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Gadidae sp.",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Gadidae sp.",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Gadidae sp.",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Gadidae sp.",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Gadidae sp.",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Gadidae sp.",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Gadidae sp.",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Gadidae sp.",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Gadidae sp.",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Gadidae sp.",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Gadidae sp.",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>GADIDAE<br></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE1<-Datataille %>% filter(Species=="Merlangius merlangus",Sexe=="M")
TAILLE11 <- TAILLE1$Fishbase
TAILLE111 <- TAILLE1$Species
TAILLE2<-Datataille %>% filter(Species=="Melanogrammus aeglefinus",Sexe=="M")
TAILLE22 <- TAILLE2$Fishbase
TAILLE222 <- TAILLE2$Species
TAILLE3<-Datataille %>% filter(Species=="Gadus morhua",Sexe=="M")
TAILLE33 <- TAILLE3$Fishbase
TAILLE333 <- TAILLE3$Species
phaseT <- c(TAILLE11,TAILLE22,TAILLE33)
phaseTT <- c(TAILLE111,TAILLE222,TAILLE333)

TT <- bind_cols(phaseTT,phaseT)

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "")
colnames(tbl_img)<-c("Sexe","Photo")

colnames(TT)<-c("Espèce","Taille de 1ère maturité")
kbl(TT, align="c") %>%
  kable_styling(full_width = F, position = "float_right")%>% column_spec(1, width="4cm")%>%kable_styling(font_size = 8)

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(2,image ="../data/Image poisson/Gadidae.jpg", width="5cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Gadidae sp.",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Gadidae sp.",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Gadidae sp.",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Gadidae sp.",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Gadidae sp.",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Gadidae sp.",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Gadidae sp.",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Gadidae sp.",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Gadidae sp.",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Gadidae sp.",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Gadidae sp.",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Gadidae sp.",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>GRONDIN ROUGE / <i>Chelidonichthys cuculus</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Chelidonichthys cuculus",Sexe=="F")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Grondin.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Chelidonichthys cuculus",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Chelidonichthys cuculus",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Chelidonichthys cuculus",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Chelidonichthys cuculus",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Chelidonichthys cuculus",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Chelidonichthys cuculus",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Chelidonichthys cuculus",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Chelidonichthys cuculus",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Chelidonichthys cuculus",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Chelidonichthys cuculus",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Chelidonichthys cuculus",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Chelidonichthys cuculus",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>GRONDIN ROUGE / <i>Chelidonichthys cuculus</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Chelidonichthys cuculus",Sexe=="M")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Grondin.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Chelidonichthys cuculus",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Chelidonichthys cuculus",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Chelidonichthys cuculus",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Chelidonichthys cuculus",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Chelidonichthys cuculus",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Chelidonichthys cuculus",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Chelidonichthys cuculus",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Chelidonichthys cuculus",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Chelidonichthys cuculus",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Chelidonichthys cuculus",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Chelidonichthys cuculus",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Chelidonichthys cuculus",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>HARENG / <i>Clupea clupea</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Clupea clupea",Sexe=="F")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/hareng.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Clupea clupea",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Clupea clupea",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Clupea clupea",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Clupea clupea",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Clupea clupea",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Clupea clupea",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Clupea clupea",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Clupea clupea",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Clupea clupea",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Clupea clupea",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Clupea clupea",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Clupea clupea",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>HARENG / <i>Clupea clupea</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Clupea clupea",Sexe=="M")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/hareng.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Clupea clupea",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Clupea clupea",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Clupea clupea",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Clupea clupea",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Clupea clupea",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Clupea clupea",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Clupea clupea",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Clupea clupea",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Clupea clupea",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Clupea clupea",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Clupea clupea",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Clupea clupea",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>MAQUEREAU / <i>Scomber scombrus</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Scomber scombrus",Sexe=="F")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Maquereau.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scomber scombrus",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scomber scombrus",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scomber scombrus",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scomber scombrus",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scomber scombrus",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scomber scombrus",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scomber scombrus",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scomber scombrus",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scomber scombrus",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scomber scombrus",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scomber scombrus",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scomber scombrus",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>MAQUEREAU / <i>Scomber scombrus</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Scomber scombrus",Sexe=="M")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Maquereau.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scomber scombrus",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scomber scombrus",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scomber scombrus",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scomber scombrus",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scomber scombrus",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scomber scombrus",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scomber scombrus",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scomber scombrus",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scomber scombrus",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scomber scombrus",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scomber scombrus",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scomber scombrus",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>MERLU / <i>Merluccius merluccius</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Merluccius merluccius",Sexe=="F")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Merlu.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Merluccius merluccius",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Merluccius merluccius",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Merluccius merluccius",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Merluccius merluccius",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Merluccius merluccius",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Merluccius merluccius",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Merluccius merluccius",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Merluccius merluccius",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Merluccius merluccius",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Merluccius merluccius",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Merluccius merluccius",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Merluccius merluccius",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>MERLU / <i>Merluccius merluccius</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Merluccius merluccius",Sexe=="M")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Merlu.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Merluccius merluccius",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Merluccius merluccius",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - DEVELOPPING</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Merluccius merluccius",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Merluccius merluccius",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Merluccius merluccius",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Merluccius merluccius",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Merluccius merluccius",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Merluccius merluccius",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Merluccius merluccius",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Merluccius merluccius",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Merluccius merluccius",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Merluccius merluccius",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>POISSONS PLATS<br></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE1<-Datataille %>% filter(Species=="Pleuronectes platessa",Sexe=="F")
TAILLE11 <- TAILLE1$Fishbase
TAILLE111 <- TAILLE1$Species
TAILLE2<-Datataille %>% filter(Species=="Lepidorhombus whiffiagonis",Sexe=="F")
TAILLE22 <- TAILLE2$Fishbase
TAILLE222 <- TAILLE2$Species
TAILLE3<-Datataille %>% filter(Species=="Microstomus kitt",Sexe=="F")
TAILLE33 <- TAILLE3$Fishbase
TAILLE333 <- TAILLE3$Species
phaseT <- c(TAILLE11,TAILLE22,TAILLE33)
phaseTT <- c(TAILLE111,TAILLE222,TAILLE333)

TT <- bind_cols(phaseTT,phaseT)

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "")
colnames(tbl_img)<-c("Sexe","Photo")

colnames(TT)<-c("Espèce","Taille de 1ère maturité")
kbl(TT, align="c") %>%
  kable_styling(full_width = F, position = "float_right")%>% column_spec(1, width="4cm")%>%kable_styling(font_size = 10)

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(2,image ="../data/Image poisson/Flat fish.jpg", width="4cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Flat fish sp.",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Flat fish sp.",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Flat fish sp.",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Flat fish sp.",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Flat fish sp.",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Flat fish sp.",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Flat fish sp.",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Flat fish sp.",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Flat fish sp.",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Flat fish sp.",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Flat fish sp.",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Flat fish sp.",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>POISSONS PLATS<br></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE1<-Datataille %>% filter(Species=="Pleuronectes platessa",Sexe=="M")
TAILLE11 <- TAILLE1$Fishbase
TAILLE111 <- TAILLE1$Species
TAILLE2<-Datataille %>% filter(Species=="Lepidorhombus whiffiagonis",Sexe=="M")
TAILLE22 <- TAILLE2$Fishbase
TAILLE222 <- TAILLE2$Species
TAILLE3<-Datataille %>% filter(Species=="Microstomus kitt",Sexe=="M")
TAILLE33 <- TAILLE3$Fishbase
TAILLE333 <- TAILLE3$Species
phaseT <- c(TAILLE11,TAILLE22,TAILLE33)
phaseTT <- c(TAILLE111,TAILLE222,TAILLE333)

TT <- bind_cols(phaseTT,phaseT)

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "")
colnames(tbl_img)<-c("Sexe","Photo")

colnames(TT)<-c("Espèce","Taille de 1ère maturité")
kbl(TT, align="c") %>%
  kable_styling(full_width = F, position = "float_right")%>% column_spec(1, width="4cm")%>%kable_styling(font_size = 10)

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(2,image ="../data/Image poisson/Flat fish.jpg", width="4cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Flat fish sp.",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Flat fish sp.",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Flat fish sp.",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Flat fish sp.",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Flat fish sp.",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Flat fish sp.",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Flat fish sp.",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Flat fish sp.",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Flat fish sp.",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Flat fish sp.",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Flat fish sp.",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Flat fish sp.",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>ROUGET BARBET DE ROCHE / <i>Mullus surmuletus</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Mullus surmuletus",Sexe=="F")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Rouget.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Mullus surmuletus",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Mullus surmuletus",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Mullus surmuletus",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Mullus surmuletus",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Mullus surmuletus",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Mullus surmuletus",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Mullus surmuletus",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Mullus surmuletus",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Mullus surmuletus",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Mullus surmuletus",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Mullus surmuletus",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Mullus surmuletus",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>ROUGET BARBET DE ROCHE / <i>Mullus surmuletus</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Mullus surmuletus",Sexe=="M")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Rouget.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Mullus surmuletus",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Mullus surmuletus",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Mullus surmuletus",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Mullus surmuletus",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Mullus surmuletus",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Mullus surmuletus",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Mullus surmuletus",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Mullus surmuletus",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Mullus surmuletus",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Mullus surmuletus",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Mullus surmuletus",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Mullus surmuletus",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>SPRAT / <i>Sprattus sprattus</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Sprattus sprattus",Sexe=="F")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Sprat.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sprattus sprattus",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sprattus sprattus",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sprattus sprattus",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sprattus sprattus",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sprattus sprattus",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sprattus sprattus",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sprattus sprattus",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sprattus sprattus",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sprattus sprattus",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sprattus sprattus",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sprattus sprattus",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sprattus sprattus",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>SPRAT / <i>Sprattus sprattus</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Sprattus sprattus",Sexe=="M")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Sprat.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sprattus sprattus",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sprattus sprattus",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sprattus sprattus",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sprattus sprattus",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sprattus sprattus",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sprattus sprattus",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sprattus sprattus",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sprattus sprattus",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sprattus sprattus",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sprattus sprattus",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sprattus sprattus",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sprattus sprattus",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>SARDINE / <i>Sardina pilchardus</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Sardina pilchardus",Sexe=="F")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Sardine.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sardina pilchardus",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sardina pilchardus",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sardina pilchardus",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sardina pilchardus",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sardina pilchardus",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sardina pilchardus",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sardina pilchardus",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sardina pilchardus",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sardina pilchardus",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sardina pilchardus",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sardina pilchardus",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sardina pilchardus",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>SARDINE / <i>Sardina pilchardus</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Sardina pilchardus",Sexe=="M")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Sardine.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sardina pilchardus",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sardina pilchardus",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sardina pilchardus",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sardina pilchardus",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sardina pilchardus",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sardina pilchardus",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sardina pilchardus",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sardina pilchardus",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sardina pilchardus",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sardina pilchardus",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Sardina pilchardus",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Sardina pilchardus",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>SOLE / <i>Solea solea</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Solea solea",Sexe=="F")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Female sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Sole.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Solea solea",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Solea solea",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Solea solea",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Solea solea",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Solea solea",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Solea solea",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Solea solea",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Solea solea",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Solea solea",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Solea solea",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Solea solea",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Solea solea",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>SOLE / <i>Solea solea</i></bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE<-Datataille %>% filter(Species=="Solea solea",Sexe=="M")
TAILLE <- TAILLE$Fishbase

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "", name = TAILLE)
colnames(tbl_img)<-c("Sexe","Photo","Taille de 1ère maturité")

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(3,bold=T)%>%column_spec(2,image ="../data/Image poisson/Sole.jpg", width="3cm")

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Solea solea",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Solea solea",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Solea solea",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Solea solea",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Solea solea",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Solea solea",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Solea solea",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Solea solea",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Solea solea",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Solea solea",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Solea solea",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Solea solea",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<h1 style="color:white;font-size:25px;background-color:#4775D1;font-weight:700;">MATURITE</h1>
<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>BARBUE ET TURBOT</bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE1<-Datataille %>% filter(Species=="Scophthalmus rhombus",Sexe=="F")
TAILLE11 <- TAILLE1$Fishbase
TAILLE111 <- TAILLE1$Species
TAILLE2<-Datataille %>% filter(Species=="Scophthalmus maximus",Sexe=="F")
TAILLE22 <- TAILLE2$Fishbase
TAILLE222 <- TAILLE2$Species
phaseT <- c(TAILLE11,TAILLE22)
phaseTT <- c(TAILLE111,TAILLE222)

TT <- bind_cols(phaseTT,phaseT)

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "")
colnames(tbl_img)<-c("Sexe","Photo")

colnames(TT)<-c("Espèce","Taille de 1ère maturité")
kbl(TT, align="c") %>%
  kable_styling(full_width = F, position = "float_right")%>% column_spec(1, width="4cm")%>%kable_styling(font_size = 10)

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(2,image ="../data/Image poisson/BarbueTurbot.jpg", width="4cm")


```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scophthalmus sp.",Phase=="A - IMMATURE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scophthalmus sp.",Phase=="A - IMMATURE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scophthalmus sp.",Phase=="B - EN DEVELOPPEMENT",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scophthalmus sp.",Phase=="B - EN DEVELOPPEMENT", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scophthalmus sp.",Phase=="C - EN PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scophthalmus sp.",Phase=="C - EN PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scophthalmus sp.",Phase=="D - REGRETION/REGENERATION",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scophthalmus sp.",Phase=="D - REGRETION/REGENERATION", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scophthalmus sp.",Phase=="E - OMISSION DE PONTE",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scophthalmus sp.",Phase=="E - OMISSION DE PONTE", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scophthalmus sp.",Phase=="F - ANORMAL",Sexe=="F")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scophthalmus sp.",Phase=="F - ANORMAL", Sexe=="F")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

-----

<h2 style="font-size:20px;background-color:#E6E6FF;"><bold>BARBUE ET TURBOT</bold></h2>

```{r , echo=F, resultas='asis', warning=F, message=F}

#Création d'un tableau avec la photo du poisson, son sexe et sa taille de 1ère maturité

#Objet TAILLE avec la taille de 1ère maturité
TAILLE1<-Datataille %>% filter(Species=="Scophthalmus rhombus",Sexe=="M")
TAILLE11 <- TAILLE1$Fishbase
TAILLE111 <- TAILLE1$Species
TAILLE2<-Datataille %>% filter(Species=="Scophthalmus maximus",Sexe=="M")
TAILLE22 <- TAILLE2$Fishbase
TAILLE222 <- TAILLE2$Species
phaseT <- c(TAILLE11,TAILLE22)
phaseTT <- c(TAILLE111,TAILLE222)

TT <- bind_cols(phaseTT,phaseT)

#Création du tableau
tbl_img <-data.frame(Espèce = "", Sexe = "")
colnames(tbl_img)<-c("Sexe","Photo")

colnames(TT)<-c("Espèce","Taille de 1ère maturité")
kbl(TT, align="c") %>%
  kable_styling(full_width = F, position = "float_right")%>% column_spec(1, width="4cm")%>%kable_styling(font_size = 10)

kbl(tbl_img, booktabs =T,align="c", valign = "c")%>%column_spec(1,image ="../data/Male sign.png")%>% kable_styling(full_width = F, position = "left")%>%column_spec(2,image ="../data/Image poisson/BarbueTurbot.jpg", width="4cm")


```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;">A - IMMATURE</h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Affichage des phases de maturité : VERSION 1

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scophthalmus sp.",Phase=="A - IMMATURE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scophthalmus sp.",Phase=="A - IMMATURE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>B - EN DEVELOPPEMENT</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scophthalmus sp.",Phase=="B - EN DEVELOPPEMENT",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scophthalmus sp.",Phase=="B - EN DEVELOPPEMENT", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>C - EN PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scophthalmus sp.",Phase=="C - EN PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scophthalmus sp.",Phase=="C - EN PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>

------

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>D - REGRETION/REGENERATION</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scophthalmus sp.",Phase=="D - REGRETION/REGENERATION",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scophthalmus sp.",Phase=="D - REGRETION/REGENERATION", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>E - OMISSION DE PONTE</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scophthalmus sp.",Phase=="E - OMISSION DE PONTE",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scophthalmus sp.",Phase=="E - OMISSION DE PONTE", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<hr style="color:#4775D1;height:5px;background-color:#4775D1;"></hr>

<h3 style="color:red;font-size:20px;font-weight:1000;"><bold>F - ANORMAL</bold></h3>

```{r , echo=F, resultas='asis', warning=F, message=F, out.width = '200px', out.extra='style="background-color: white; padding:2px; display: inline-block;"'}

#Extraction des commentaires liées a la phase
textASB<-Datascale %>% filter(Species=="Scophthalmus sp.",Phase=="F - ANORMAL",Sexe=="M")
textASB <- textASB$Text

#Création d'un tableau regroupant les commentaires
tab <- data.frame(x = textASB)
colnames(tab) <- c(" ")

#Partie de sélection des photos

#Extraction des photos correspondant a la phase
ASB<-Dataphoto %>% filter(Species=="Scophthalmus sp.",Phase=="F - ANORMAL", Sexe=="M")
ASBType <- filter(ASB,Fiches=='oui'| Fiches=='na')

permutation <- order(ASBType$Fiches, decreasing = TRUE)
ASBType <- head(ASBType[permutation,])

if(ASBType$Fiches=='oui') {
 ASBType <- filter(ASBType, Fiches != "na") 
}
ASBType<-ASBType$Link

#Insertion des photos
kbl(tab) %>%
  kable_styling(full_width = F, position = "float_left")%>% column_spec(1, width="4cm")
knitr::include_graphics(ASBType)

```

<h4 style="font-size:15px;color:#4775D1;">Références/Photos</h4>
<hr style="color:#4775D1;border-bottom: 1px dashed #4775D1;"></hr>
<p>CIEM (<bold>WKASMSF</bold>2018 & <bold>WKMATCH</bold> 2012)<br> IFREMER (CGFS, EVHOE, IBTS) </br></p>


</body>
</html> 

